#ifndef STINGRAYKIT_CHUNKEDBYTEDATA_H
#define STINGRAYKIT_CHUNKEDBYTEDATA_H

#include <stingraykit/collection/ByteData.h>
#include <stingraykit/collection/IEnumerable.h>


namespace stingray
{

	STINGRAYKIT_DECLARE_ENUMERABLE(ByteArray);
	STINGRAYKIT_DECLARE_ENUMERABLE(ByteData);
	STINGRAYKIT_DECLARE_ENUMERABLE(ConstByteArray);
	STINGRAYKIT_DECLARE_ENUMERABLE(ConstByteData);

}


#endif
